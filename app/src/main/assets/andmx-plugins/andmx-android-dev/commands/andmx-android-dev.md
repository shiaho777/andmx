---
description: Start the AndMX Android development loop.
argument-hint: "[goal or issue description]"
skills: andmx-android-dev
---

Use the `andmx-android-dev` skill for this request:

$ARGUMENTS

Start with `android_preflight`. If setup is missing, follow `skills/andmx-android-dev/INSTALL_ENVIRONMENT.md`, re-run `android_preflight`, then discover or create the Android project, build and launch it on emulator/device, and verify with a screenshot.
