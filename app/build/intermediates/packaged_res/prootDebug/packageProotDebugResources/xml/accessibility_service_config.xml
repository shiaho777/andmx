<?xml version="1.0" encoding="utf-8"?>
<!--
  AccessibilityService config for Computer Use.

  We request the ability to retrieve window content (so typeText can find the
  focused editable) and to perform gestures (tap/swipe). We do NOT enable
  event reporting, flagsRequestEnhancedWebAccessibility, or canRequestFilterKeyEvents
  — the service stays pure-visual and low-overhead.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="100" />
